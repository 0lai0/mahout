# B2 — Fail fast when estimated memory exceeds VRAM

**GitHub title:** `[pipeline][gsoc] Fail fast when estimated memory exceeds VRAM`

**Labels:** `qdp`, `pipeline`, `gsoc`

**Depends on:** [B1](B1.md)

**Parent roadmap:** #___

---

## Scope

- Call `estimate_memory` inside `PipelineIterator::new_*` (or unified `new` after [E1](E1.md)) and Python loader build path
- Query free GPU memory via existing CUDA helpers
- Return actionable error, e.g. suggest reducing `batch_size`, `num_qubits`, or `prefetch_depth`

## Files expected

- `qdp-core/src/pipeline_runner.rs`
- `qdp-python/src/loader.rs` and/or `qumat_qdp/loader.py`

## Acceptance criteria

- [ ] Intentionally oversized config fails in under 1 second with clear message
- [ ] All existing valid configs unchanged
- [ ] Optional: expose `estimate_memory` on Python for dry-run preview (nice-to-have)
