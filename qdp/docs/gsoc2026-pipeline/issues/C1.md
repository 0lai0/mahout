# C1 — PyO3 bindings for Preprocessor

**GitHub title:** `[pipeline][gsoc] PyO3 bindings for Preprocessor`

**Labels:** `qdp`, `pipeline`, `gsoc`, `good first issue`

**Depends on:** —

**Parent roadmap:** #___

---

## Problem

`Preprocessor` exists in Rust (`qdp-core/src/preprocessing.rs`) but is not exposed to Python (`qumat_qdp`).

## Scope

- Expose via PyO3: `validate_input`, `calculate_l2_norm`, and existing normalize/standardize helpers if present in Rust
- f64 first; f32 follow-up if Rust API is still f64-only at time of work

## Files expected

- `qdp-python/src/` (new module or extend existing bindings)
- `qdp-core/src/preprocessing.rs` (only if small API adjustments needed)

## Acceptance criteria

- [ ] `python -c "from qumat_qdp import ..."` smoke test succeeds
- [ ] Pytest covering at least one validation-error path
