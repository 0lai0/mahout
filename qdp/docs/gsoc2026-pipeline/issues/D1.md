# D1 — KernelElem trait to deduplicate f32/f64 stubs

**GitHub title:** `[kernels][gsoc] KernelElem trait to deduplicate f32/f64 stubs`

**Labels:** `qdp`, `kernels`, `gsoc`

**Depends on:** — (coordinate with #1310 to avoid overlap on GPU trait work)

**Parent roadmap:** #___

---

## Problem

- `qdp-kernels/src/lib.rs` (~726 lines): ~270 lines are copy-paste stubs for every `*_f32` FFI variant
- `qdp-core/src/gpu/encodings/angle.rs`: `encode_batch_async_pipeline` and `encode_batch_async_pipeline_f32` are ~60-line near-duplicates

## Scope

- New `qdp-kernels/src/elem.rs`: `KernelElem` trait with `impl` for `f32` and `f64`
- Replace stub block with macro or generic dispatch (C ABI stays dual)
- Collapse duplicate `encode_batch_async_pipeline*` in `angle.rs` where safe

**Out of scope:** Changing CUDA kernel algorithms

## Files expected

- `qdp-kernels/src/elem.rs`, `qdp-kernels/src/lib.rs`
- `qdp-core/src/gpu/encodings/angle.rs`

## Acceptance criteria

- [ ] `cargo test -p qdp-kernels` and `qdp-core` GPU tests pass
- [ ] Net LoC reduction
- [ ] Throughput regression ≤2% on amplitude f32 baseline
